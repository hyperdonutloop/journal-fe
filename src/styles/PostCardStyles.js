import styled from 'styled-components';

export const Wrapper = styled.div`
  .post-card {
	  .date {
      font-size: 1.8em;
      line-height: 1em;
      margin-bottom: 20px;
    }
    
    .weekday {
      display: flex;
    }

	.post-title {
    font-size: 1.2em;
    margin-top: 1rem;
		/* font-family: $font-content; */
	}
}
`;